<nav>
    <ul>

        <li><a href="{{ path('main_home') }}" title="Accueil">Accueil</a></li>

        {% if app.user %}
            <li><a href="{{ path('main_profil') }}" title="monProfil">Mon Profil</a></li>
            <li><a href="{{ path('app_logout') }}" title="seDeconnecter">Se Déconnecter ({{ app.user.userIdentifier }})</a></li>
        {% endif %}

        {% if is_granted("ROLE_ADMIN") %}
            <li><a href="{{ path('main_campus') }}" title="Campus">Campus</a></li>
            <li><a href="{{ path('main_villes') }}" title="Villes">Ville</a></li>
        {% endif %}
    </ul>
</nav>